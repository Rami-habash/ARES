[pytest]
# Load backend/.env into the test process so ARES_DB_URL / ARES_TEST_DB_URL
# are available to conftest.py without needing to export them in the shell.
env_files =
    .env
testpaths = tests
